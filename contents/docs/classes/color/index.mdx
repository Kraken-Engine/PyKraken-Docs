---
title: Color
description: Represents an RGBA color.
---

## Constructor
<div className="api-card">

- <ApiSig name="Color" returns="Color" />
- <ApiSig name="Color" multiline params={[{ name: "r", type: "SupportsInt" }, { name: "g", type: "SupportsInt" }, { name: "b", type: "SupportsInt" }, { name: "a", type: "SupportsInt", default: "255" }]} returns="Color" />
- <ApiSig name="Color" params={[{ name: "hex", type: "str" }]} returns="Color" />
- <ApiSig name="Color" params={[{ name: "sequence", type: "Sequence" }]} returns="Color" />

Represents an RGBA color.
</div>

## Properties
<hr style={{marginBottom: 0}} />

| Name | Description | Type |
| --- | --- | --- |
| `a` | Alpha (transparency) channel value. | <code>int</code> |
| `b` | Blue channel value. | <code>int</code> |
| `g` | Green channel value. | <code>int</code> |
| `hex` | Get or set the color as a hex string. | <code>str</code> |
| `hsv` | Get or set the color as an HSV tuple. | <code>tuple[float, float, float, float]</code> |
| `r` | Red channel value. | <code>int</code> |

## Methods
---

### Copy
<div className="api-card">
<ApiSig name="copy" returns="Color" />

Create a copy of the color.

_**Returns**_

`Color` : A new Color object with the same RGBA values.

</div>
