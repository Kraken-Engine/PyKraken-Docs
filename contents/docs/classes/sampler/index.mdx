---
title: Sampler
description: Encapsulates a GPU sampler object used by shaders. Access via the 'shaders' submodule.
---

## Constructor
<div className="api-card">

- <ApiSig name="Sampler" multiline params={[{ name: "min_filter", type: "FilterMode", default: "FilterMode.DEFAULT" }, { name: "mag_filter", type: "FilterMode", default: "FilterMode.DEFAULT" }, { name: "wrap_u", type: "WrapMode", default: "WrapMode.CLAMP" }, { name: "wrap_v", type: "WrapMode", default: "WrapMode.CLAMP" }]} returns="Sampler" />

Encapsulates a GPU sampler object used by shaders.
</div>
