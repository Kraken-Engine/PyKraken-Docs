---
title: Camera
description: Represents a 2D camera used for rendering.
---

## Constructor
<div className="api-card">

- <ApiSig name="Camera" returns="Camera" />
- <ApiSig name="Camera" params={[{ name: "pos", type: "Vec2" }]} returns="Camera" />
- <ApiSig name="Camera" params={[{ name: "x", type: "SupportsFloat" }, { name: "y", type: "SupportsFloat" }]} returns="Camera" />

Represents a 2D camera used for rendering.
</div>

## Properties
<hr style={{marginBottom: 0}} />

| Name | Description | Type |
| --- | --- | --- |
| `pos` | Get or set the camera's position. | <code><a href="/docs/classes/vec2">Vec2</a></code> |

## Methods
---

### Set
<div className="api-card">
<ApiSig name="set" returns="None" />

Set this camera as the active one for rendering.

Only one camera can be active at a time.

</div>
