---
title: Camera
description: Represents a 2D camera used for rendering.
---

## Constructor
<div className="api-card">

- <ApiSig name="Camera" params={[{ name: "set_active", type: "bool", default: "False" }]} returns="Camera" />

Represents a 2D camera used for rendering.
</div>

## Properties
<hr style={{marginBottom: 0}} />

| Name | Description | Type |
| --- | --- | --- |
| `transform` | The camera transform. `transform.pos` is the world point at the center of the view. | <code><a href="/docs/classes/transform">Transform</a></code> |

## Methods
---

### Move Screen
<div className="api-card">
<ApiSig name="move_screen" params={[{ name: "delta", type: "Vec2" }]} returns="None" />

Move the camera by a delta in screen/camera space.

_**Args**_

- `delta` : Screen-space movement delta.

</div>

### Move World
<div className="api-card">
<ApiSig name="move_world" params={[{ name: "delta", type: "Vec2" }]} returns="None" />

Move the camera by a delta in world space.

_**Args**_

- `delta` : World movement delta.

</div>

### Rotate
<div className="api-card">
<ApiSig name="rotate" params={[{ name: "delta", type: "float" }]} returns="None" />

Rotate the camera view by a delta in radians.

_**Args**_

- `delta` : Angle delta in radians.

</div>

### Screen To World
<div className="api-card">
<ApiSig name="screen_to_world" params={[{ name: "screen_pos", type: "Vec2" }]} returns="Vec2" />

Convert a screen position to a world position using this camera.

_**Args**_

- `screen_pos` : The screen position to convert.

_**Returns**_

`Vec2` : The resulting world position.

</div>

### Set
<div className="api-card">
<ApiSig name="set" returns="None" />

Set this camera as the active one for rendering.

Only one camera can be active at a time.

</div>

### Unset
<div className="api-card">
<ApiSig name="unset" returns="None" />

Unset this camera as the active one for rendering.

</div>

### World To Screen
<div className="api-card">
<ApiSig name="world_to_screen" params={[{ name: "world_pos", type: "Vec2" }]} returns="Vec2" />

Convert a world position to a screen position using this camera.

_**Args**_

- `world_pos` : The world position to convert.

_**Returns**_

`Vec2` : The resulting screen position.

</div>
