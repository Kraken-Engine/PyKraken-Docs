---
title: Transform
description: Functions for transforming pixel arrays.
---

---

## Flip
<div className="api-card">

<ApiSig name="flip" params={[{ name: "pixel_array", type: "PixelArray" }, { name: "flip_x", type: "bool" }, { name: "flip_y", type: "bool" }]} returns="PixelArray" />

Flip a pixel array horizontally and/or vertically.

_**Parameters**_

- `pixel_array` : The pixel array to flip.
- `flip_x` : Whether to flip the pixel array horizontally.
- `flip_y` : Whether to flip the pixel array vertically.

_**Returns**_

`PixelArray` : The flipped pixel array.

</div>

## Scale To
<div className="api-card">

<ApiSig name="scale_to" params={[{ name: "pixel_array", type: "PixelArray" }, { name: "size", type: "Vec2" }]} returns="PixelArray" />

Scale a pixel array to a specific size.

_**Parameters**_
- `pixel_array` : The pixel array to scale.
- `size` : The size to scale the pixel array to.

_**Returns**_

`PixelArray` : The scaled pixel array.

</div>

## Scale By
<div className="api-card">

<ApiSig name="scale_by" params={[{ name: "pixel_array", type: "PixelArray" }, { name: "factor", type: "float" }]} returns="PixelArray" />

Scale a pixel array by a specific factor.

_**Parameters**_

- `pixel_array` : The pixel array to scale.
- `factor` : The factor by which to scale the pixel array.

_**Returns**_

`PixelArray` : The scaled pixel array.

</div>

## Rotate
<div className="api-card">

<ApiSig name="rotate" params={[{ name: "pixel_array", type: "PixelArray" }, { name: "angle", type: "float" }]} returns="PixelArray" />

Rotate a pixel array by a specific angle.

_**Parameters**_

- `pixel_array` : The pixel array to rotate.
- `angle` : The angle in degrees to rotate the pixel array.

_**Returns**_

`PixelArray` : The rotated pixel array.

</div>

## Box Blur
<div className="api-card">

<ApiSig name="box_blur" params={[{ name: "pixel_array", type: "PixelArray" }, { name: "radius", type: "int" }, { name: "repeat_edge_pixels", type: "bool", default: "True" }]} returns="PixelArray" />

_**Parameters**_

- `pixel_array` : The pixel array to blur.
- `radius` : The radius of the blur.
- `repeat_edge_pixels` : Whether to repeat edge pixels when blurring.

_**Returns**_

`PixelArray` : The blurred pixel array.

</div>

## Gaussian Blur
<div className="api-card">

<ApiSig name="gaussian_blur" params={[{ name: "pixel_array", type: "PixelArray" }, { name: "radius", type: "int" }, { name: "repeat_edge_pixels", type: "bool", default: "True" }]} returns="PixelArray" />

_**Parameters**_

- `pixel_array` : The pixel array to blur.
- `radius` : The radius of the blur.
- `repeat_edge_pixels` : Whether to repeat edge pixels when blurring.

_**Returns**_

`PixelArray` : The blurred pixel array.

</div>

## Invert
<div className="api-card">

<ApiSig name="invert" params={[{ name: "pixel_array", type: "PixelArray" }]} returns="PixelArray" />

Invert the colors of a pixel array.

_**Parameters**_

- `pixel_array` : The pixel array to invert.

_**Returns**_

`PixelArray` : The color-inverted pixel array.

</div>

## Grayscale
<div className="api-card">

<ApiSig name="grayscale" params={[{ name: "pixel_array", type: "PixelArray" }]} returns="PixelArray" />

Convert a pixel array to luma grayscale.

_**Parameters**_
- `pixel_array` : The pixel array to convert to grayscale.

_**Returns**_

`PixelArray` : The grayscale pixel array.

</div>
