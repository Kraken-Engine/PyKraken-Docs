---
title: Window
description: Functions for creating and managing an application window.
---

---

## Create
<div className="api-card">

<ApiSig name="create" params={[{ name: "title", type: "str" }, { name: "resolution", type: "Vec2" }, { name: "scaled", type: "bool", default: "False" }]} returns="None" />

Create a new application window.

_**Parameters**_

- `title` : The title of the window.
- `resolution` : The initial resolution of the window.
- `scaled` : Whether the window should be automatically scaled up to fit into the display.

</div>

## Is Open
<div className="api-card">

<ApiSig name="is_open" returns="bool" />

Check if the window is currently open.

_**Returns**_

`bool` : True if the window is open, False otherwise.

</div>

## Close
<div className="api-card">

<ApiSig name="close" returns="None" />

Close the application window.

</div>

## Get Size
<div className="api-card">

<ApiSig name="get_size" returns="Vec2" />

Get the current size of the window. If scaled, the size will be adjusted to fit the display.

_**Returns**_

`Vec2` : The current size of the window.

</div>

## Set Fullscreen
<div className="api-card">

<ApiSig name="set_fullscreen" params={[{ name: "fullscreen", type: "bool" }]} returns="None" />

Set the window to a fullscreen mode.

_**Parameters**_

- `fullscreen` : Whether to enable or disable fullscreen mode.

</div>

## Is Fullscreen
<div className="api-card">

<ApiSig name="is_fullscreen" returns="bool" />

Check if the window is currently in fullscreen mode.

_**Returns**_

`bool` : True if the window is in fullscreen mode, False otherwise.

</div>

## Set Title
<div className="api-card">

<ApiSig name="set_title" params={[{ name: "title", type: "str" }]} returns="None" />

Set the title of the window.

_**Parameters**_

- `title` : The new title of the window.

</div>

## Get Title
<div className="api-card">

<ApiSig name="get_title" returns="str" />

Get the current title of the window.

_**Returns**_

`str` : The current title of the window.

</div>

## Get Scale
<div className="api-card">

<ApiSig name="get_scale" returns="int" />

Get the scale of the window relative to the renderer resolution.

_**Returns**_

`int` : The current scale factor of the window.

</div>

## Save Screenshot
<div className="api-card">

<ApiSig name="save_screenshot" params={[{ name: "path", type: "str" }]} returns="None" />

Save a screenshot of the current window content to a file.

_**Parameters**_

- `path` : The file path where the screenshot will be saved.

</div>